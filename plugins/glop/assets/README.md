# Assets

Pendientes de diseño. `plugin.json` referencia:

- `icon.png`: icono del compositor (cuadrado, fondo transparente).
- `logo.png`: logo del listado (cuadrado, mínimo 512×512).

No se suben capturas (`screenshots`) porque el plugin no tiene UI propia; OpenAI pide no
adjuntarlas en ese caso.
